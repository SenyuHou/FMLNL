name: fmlnl
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch==2.5.1+cu121
      - torchvision==0.20.1+cu121
      - --extra-index-url https://download.pytorch.org/whl/cu121
      - timm
      - open_clip_torch
      - ftfy
      - regex
      - numpy
      - scipy
      - scikit-learn
      - pandas
      - pyyaml
      - tqdm
