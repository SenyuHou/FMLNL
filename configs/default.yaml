data_root: data
features_root: features
output_dir: outputs/generalization
backbone: vit_b16
batch_size: 256
num_workers: 4
device: auto
hf_endpoint: ""
seeds: [1, 2, 3]
global_posterior_threshold: 0.8
local_posterior_threshold: 0.5
knn_k: 20
